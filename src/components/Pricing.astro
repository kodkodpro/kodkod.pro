---
const tiers = [
  {
    price: 5,
    name: "Джун"
  },
  {
    price: 10,
    name: "Мидл"
  },
  {
    price: 15,
    name: "Про"
  }
]
---

<div class="container mx-auto">
  <div class="grid grid-cols-3 gap-4">
    {tiers.map((tier) => (
      <div class="text-center bg-neutral-100 p-6 rounded-xl">
        <div class="text-3xl font-serif font-bold mb-2">{tier.name}</div>
        <div class="text-2xl font-serif font-bold">${tier.price}</div>
        <div class="text-sm text-neutral-500 font-medium">/месяц</div>
      </div>
    ))}
  </div>
</div>